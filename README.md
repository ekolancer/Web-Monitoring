WEB-MON BNPB — Modular Refactor

Setup:
1. Buat virtualenv: python -m venv venv && source venv/bin/activate
2. pip install -r requirements.txt
3. Letakkan credentials.json (Service Account) di root project
4. Edit config.py (telegram, spreadsheet name jika perlu)

Run:
python main.py
